<%= form_for @file, remote: true do |f| %>
  <% if @file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file.errors.count, "error") %> prohibited this file from being saved:</h2>

      <ul>
      <% @file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <%= f.hidden_field :directory_id, value: params[:id] %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= javascript_tag render template: 'files/create_file.js.erb.coffee' %>